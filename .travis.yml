sudo: required
services:
  - docker

before_install:
  - docker build -t cks/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true cks/react-test npm test

after_success:
  - docker build -t cks/multi-client ./client
  - docker build -t cks/multi-nginx ./nginx
  - docker build -t cks/multi-server ./server
  - docker build -t cks/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push cks/multi-client
  - docker push cks/multi-nginx
  - docker push cks/multi-server
  - docker push cks/multi-worker
