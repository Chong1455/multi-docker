sudo: required
services:
  - docker

before_install:
  - docker build -t chongks/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true chongks/react-test npm test

after_success:
  - docker build -t chongks/multi-client ./client
  - docker build -t chongks/multi-nginx ./nginx
  - docker build -t chongks/multi-server ./server
  - docker build -t chongks/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push chongks/multi-client
  - docker push chongks/multi-nginx
  - docker push chongks/multi-server
  - docker push chongks/multi-worker
